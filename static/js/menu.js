
  $('.menu__li--X').click(()=>{
    $('#check').prop('checked',false);
  });




	// $menu = $('.cart');
  // $menu.hover(function() {
  // 		$(this).children('ul').stop();
  // 		$(this).children('ul').slideDown();
  // 	}, function() {
  // 		$(this).children('ul').stop();
  // 		$(this).children('ul').slideUp();
  // 	});
  // $(document).click((e)=>{
  //     let isChecked=$('#check:checked').length,
  //     classTarget=e.target.className;
  //     if(isChecked >0 && (classTarget === '')){
  //       $('#check').prop('checked',false);
  //     }
  // });
